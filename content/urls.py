"""
Content app URLs.

This project now serves:
- /products/ via the `products` app
- /contact/  via the `contact` app

We keep this file to avoid breaking the include in mcqplatform/urls.py, but
currently it does not register any public routes.
"""

urlpatterns = []
